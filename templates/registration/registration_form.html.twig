{% extends 'base.html.twig' %}

{% block body %}
    <h2 style="text-align: center">Register your account to enjoy the best of wild series</h2>
{{ form_start(form) }}
    {{ form_widget(form) }}
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
{% endblock %}
